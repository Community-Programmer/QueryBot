[project]
name = "querybot-server"
version = "0.1.0"
description = "QueryBot Flask Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.0",
    "langchain>=1.0.3",
]
